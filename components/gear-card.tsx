import Image from "next/image"; import Link from "next/link"; import { Star } from "lucide-react"; import type { Gear } from "@/lib/types"; import { money } from "@/lib/utils";
export function GearCard({gear}:{gear:Gear}){return <article className="card gear-card"><div style={{position:"relative"}}><Image src={gear.image} alt={gear.name} width={600} height={420}/><span className="badge" style={{position:"absolute",top:12,left:12}}>{gear.available?"Available":"Unavailable"}</span></div><div className="gear-body"><div className="gear-meta"><span>{gear.category} · {gear.brand}</span><span><Star size={13} fill="currentColor"/> {gear.rating}</span></div><h3>{gear.name}</h3><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><span><span className="price">{money(gear.pricePerDay)}</span><small className="muted"> / day</small></span><Link href={`/gear/${gear.id}`} className="btn btn-outline">View</Link></div></div></article>}
