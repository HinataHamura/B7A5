"use client";import { Button } from "@/components/ui";export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="auth-wrap"><div className="card auth-card"><span className="badge">Something went wrong</span><h1>We hit an unexpected snag</h1><p className="muted">{process.env.NODE_ENV==="development"?error.message:"Please try again. If the problem continues, contact support."}</p><Button onClick={reset}>Try again</Button></div></div>}
