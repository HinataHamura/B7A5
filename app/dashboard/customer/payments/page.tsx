"use client";import { useQuery } from "@tanstack/react-query";import { getPayments } from "@/lib/api";import { money } from "@/lib/utils";import { Card } from "@/components/ui";
export default function Payments(){const {data=[],isLoading}=useQuery({queryKey:["payments"],queryFn:getPayments});return <><h1 className="section-title">Payment history</h1><Card><div className="table-wrap"><table className="table"><thead><tr><th>Payment</th><th>Rental</th><th>Date</th><th>Amount</th><th>Status</th><th>Transaction</th></tr></thead><tbody>{isLoading?<tr><td>Loading…</td></tr>:data.map(p=><tr key={p.id}><td>{p.id}</td><td>{p.rentalId}</td><td>{p.date}</td><td>{money(p.amount)}</td><td><span className="badge">{p.status}</span></td><td>{p.transactionId}</td></tr>)}</tbody></table></div></Card></>}
