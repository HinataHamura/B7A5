import Link from "next/link";import { CheckCircle } from "lucide-react";import { Card } from "@/components/ui";
export default async function Success({searchParams}:{searchParams:Promise<{session_id?:string}>}){const {session_id}=await searchParams;return <div className="auth-wrap"><Card className="auth-card" style={{textAlign:"center"}}><CheckCircle size={64} color="#18794e" style={{margin:"auto"}}/><h1>Payment successful</h1><p className="muted">Your payment was received and the rental is now ready for the provider to fulfill.</p>{session_id&&<small>Stripe session: {session_id.slice(0,18)}…</small>}<Link className="btn" href="/dashboard/customer">View my rentals</Link></Card></div>}
