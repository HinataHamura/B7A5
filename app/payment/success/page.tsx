import Link from "next/link";import { CheckCircle } from "lucide-react";import { Card } from "@/components/ui";import { PaymentConfirmation } from "@/components/payment-confirmation";
export default async function Success({searchParams}:{searchParams:Promise<{session_id?:string}>}){const {session_id}=await searchParams;return <div className="auth-wrap"><Card className="auth-card" style={{textAlign:"center"}}><CheckCircle size={64} color="#18794e" style={{margin:"auto"}}/><h1>Payment successful</h1><PaymentConfirmation sessionId={session_id}/>{session_id&&<small>Stripe session: {session_id.slice(0,18)}…</small>}<Link className="btn" href="/dashboard/customer">View my rentals</Link></Card></div>}
